# 命名規則
・snake_case CamelCaseはClassで使用する。
・文字から始める。
・文字、数字、_を使う。
・Case sensitive　大文字と小文字を区別する。
・変数名は大文字の場合、書き換えNGの意味

# Lists / Dictionary / Tuple
*Lists: ()...[1, 2, 3, 4] etc
  後から変更可能
　[]はインデックスにアクセスする際に使用
*Dictionary:{}...{'key1: 'value1, 'key2': 2, 3: 'value3'} etc
　後から変更、代入可能
*Tuple: ()...(1, 2, 3) etc
  後から変更、代入不可

# Noneとundefined
*Noneは代入すると定義されているのでエラーにならない。
 定義されていないとundefinedとエラーが表示される。
*returnするものがない時はNoneになる
